procedure principal is
    x,y,z  : integer;
    --hola
    h :  boolean;
 function x(e: in integer; z: out float) return integer is
    x: integer;
   begin
   return s;
 end x;
 
 procedure z  is
    t:integer;
  begin
    x :=(5%+z);
    z:= (5**10) +20;
    funcion(h,x,t)
    get(t);

     if (x+20) <= 67 then
      put("hola mundo");
      
     elsif (x+5)= 20 then
       get(x);

     else
      x:=67;
    end if;
 end z;


 begin
    while x = (y+z) loop
      h(x,y,g,h);
      exit when t = 67 ;
    end loop;

    loop
     put("x");
    end loop;

    for i in 1..2+p(3,5) loop
     put("hola");
    end loop;  
  put(x);

 end principal;